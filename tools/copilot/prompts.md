prompts.md (short excerpt)
- Feature scaffold prompt: “Create a Node.js Express route POST /api/name-object that validates input {id, name, metadata}, stores metadata in a JSON schema, and returns 201 with the created record. Include unit tests using Jest.”
- Test prompt: “Generate Jest unit tests for the above route covering success and validation errors.”
